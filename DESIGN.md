# dsh-tree

把一个对话的所有分支画成一棵树，贴在聊天区右缘。点一个节点跳到那一轮，点 ＋ 从那儿接着问。

只依赖 dsh 本身。装了就能用，删掉也什么都不坏。

---

## 1. 装 / 卸

装出去是两个文件：`index.js`（跑在 dsh 进程里）和 `client.js`（跑在浏览器里）。
源码分在 `src/host/` 和 `src/client/`；`client.js` 是 `npm run build` 从后者拼出来的
**生成物**（dsh 规定"一个包 = 一个 bundle"，浏览器半只能是一个文件）。
文件布局和施工规矩见 [ARCHITECTURE.md](ARCHITECTURE.md)。

**装**：往 `$DSH_HOME/profiles/web/cordis.patch.yml` 里加三行

```yaml
- insert:
    - id: dsh-tree
      name: 'file:///绝对路径/dsh-tree/index.js'
```

**卸**：把这三行删掉，重启 dsh。

改了 host 半要重启 dsh；只改浏览器半 `npm run build` 再刷新页面就行（插件在 profile 目录之外，host 半没有热重载）。

**它在仓库外留下的东西**，一共两处：

| 什么 | 在哪 | 卸载后 |
|---|---|---|
| 安装条目 | `cordis.patch.yml` 的三行 | 自己删 |
| 节点自定义名字 | 浏览器 `localStorage['dsh-tree.labels']` | 留着，无害（MVP 妥协，以后该挪到 `ctx.storage.domain`） |

另外，**只有在装了 `dsh-claude` 的情况下**，插件会往
`$DSH_HOME/plugins/dsh-claude/sessions/<会话id>.json` 写文件（见 §4）。写的是那个插件
自己也会写的东西，只是提前写好；卸载 dsh-tree 后这些文件照常被 dsh-claude 使用。

---

## 2. 术语

| 叫法 | 是什么 | 在 dsh 里对应 |
|---|---|---|
| **对话** | 一整棵树 | 一组有血缘关系的 session |
| **分支** | 树上的一条链 | 一个 session |
| **节点** | 一问一答 | 一个 turn |
| **空节点** | 树根那个虚点 | 无，纯 UI |
| **压缩节点** | 发生过上下文压缩的那一轮 | `compaction/end` 事件 |
| **岔路点** | 一条分支是从父分支第几轮分出来的 | 继承前缀里最后一个 `turn/end` |

---

## 3. 数据从哪来 —— 树是算出来的，不是存出来的

| 信息 | 来源 |
|---|---|
| 分支之间的父子边 | session header 的 `parentSession` |
| 分支里有哪些轮 | 自己折日志（`turn/start` / `user/message` / `turn/end`） |
| 岔路点 | `session/end-seed {inherited:true}` 之前的最后一个 `turn/end` |
| 哪几轮被撤回了 | dsh-claude 旁车里的 `rewind.ranges`（见下） |
| 那一轮答完了没 | `turn/end` 的 `reason.kind === 'completed'` |
| 现在滑到哪一轮 | DOM 上的 `[data-chat-turn]` |
| 节点自定义名字 | localStorage（唯一自研存储） |

好处：树永远和真实状态一致（它就是真实状态的一个视图），插件删了什么都不坏，不需要迁移。

### 撤回（rewind）：日志里还在，对话里已经没了

John 报的：「1-2-3-4，4 发到一半我撤回了，又发了 5，树上却画成 1-2-3-4-5 —— 可 4 已经不存在了。」

原因是 dsh-claude 的撤回**不删日志、也不开新会话**（NATIVE-BASELINE.md §4）：
它只在自己的旁车里记一组 hidden `ranges`（**界面行**的 seq 区间），前端拿 CSS
把那些行藏起来，claude 那边用 `resumeSessionAt` 从更早的锚点重开。
于是我们折日志时，撤回过的那几轮一个不少地还在，而且后来发的轮次顺理成章地接在它后面。

**两种情形要分开**（`turn/end` 的 `reason` 说了算）：

| 撤回时的状态 | 树上怎么画 |
|---|---|
| 答完了（`completed`） | 节点留着，成一条**走过又被放弃的支线**；新发的那轮接回撤回**之前**那个节点 → 1-2-3-4 和 1-2-3-5 两条 |
| 没答完（`aborted` / `interrupted` / `error` / 没有 `turn/end`） | 节点**不画**，只剩 1-2-3-5 |

实现分在两半：host 半（`src/host/rewind.js`）负责盖 `rewound` 戳，浏览器半的 `buildGraph` 负责成形
（`previous` 串废弃支线，`live` 指还在对话里的最后一个节点，新轮次接 `live`）。

⚠️ **`done` 不能退化成"有没有 `turn/end`"**。盘上 34 条 `aborted` **都老老实实带着 `turn/end`**，
只是 `reason.kind` 不是 `completed`。按有无判的话，中止掉的半截轮次会被当成答完了留在树上。

⚠️ **判"这一轮撤回没"要拿真人那条消息的 seq 去比**，不是 `turn/start` 的。
撤回点就是用户点的那一行，区间从它开始 —— `turn/start` 恒落在区间起点之前，永远判不中。
折不出提示词的轮次退而用 `turn/end` 的 seq。

⚠️ **撤回状态的缓存不能挂在会话 `revision` 上**。撤回**一个 dsh 事件都不写**，
`snapshot.revision` 纹丝不动，挂上去就永远刷不出来。所以另开一个 `hiddenCache`，
跟着旁车文件的 `mtime + size` 走。

#### 🚨 读旁车会打死正在跑的那一轮

**这是这个插件里最容易把别人弄坏的一处，规矩不许放宽。**

Windows 上，**只要有任何别的句柄开着目标文件，`rename` 覆盖它就是 EPERM**。
三行就能复现：

```js
const fd = openSync(target, 'r')     // 开着读句柄
renameSync(tmp, target)              // → EPERM: operation not permitted
```

而 dsh-claude 每 150ms（`TEXT_FLUSH_MS`）就要把对话正文原子落盘一次：写 `.tmp` →
`rename` 盖掉旁车。那个 `rename` 抛出来的异常会从它的消息泵（`#pump`）里冒出去，
被 `#handleDisconnect` 当成「Claude Code 掉线」——**整轮当场判失败，而且它故意不重发**
（那一轮已经动过文件、提过 git，重放会重复副作用）。

2026-09-20 真炸过一次。第一版的实现在每次 `/outlines` 里都 `readFileSync` 一遍
6.3MB 的旁车，句柄要开约 10ms，对面每 150ms 一次 rename ——
**每读一次就有约 7% 的概率打死正在跑的那一轮**：

```
turn/end  {"kind":"error","error":{"message":"Claude Code exited after activity;
           side-effect outcome is unknown and the prompt was not replayed"}}
detail    EPERM: operation not permitted, rename '<旁车>.<pid>.<uuid>.tmp' -> '<旁车>'
```

所以规矩是：**这条会话有轮次在跑，就一个字节都不许读。**

| | |
|---|---|
| 判据 | `ctx.agents.get(id).status === 'running'`（权威）。冷会话没有 agent，没人写它，随便读 |
| 加一道静默期 | 旁车 `mtime` 1.5s 内动过也不碰，挡住 `turn/end` 之后那次迟到的 flush |
| 认不出来怎么办 | **一律当成在跑**。宁可这一轮不显示撤回，也不能赌一把 |
| 没读成怎么办 | 沿用上一次读到的，并把 `rewindPending` 报给前端 |

沿用旧值是安全的：**撤回只发生在两轮之间**（按钮在历史消息行上），所以一轮跑着的时候
缓存必然还是对的。唯一会错的是「撤回完紧接着发下一轮」——那一轮里树上画的还是撤回前的
形状。前端收到 `rewindPending` 会隔 2s 回来拉一次（撤回不写 dsh 日志，会话列表毫无动静，
**不自己回来拉就永远等不到**），并在导轨上沿挂一个 ⏳，鼠标停上去说明原因。

> ⚠️ 别改成「把读的时间缩短就行」（只读文件尾、只读前 64 字节……）：窗口变小不等于没有，
> 而代价是整轮对话当场失败。
>
> ⚠️ 也别改成只看 `mtime` 静默期：一轮里跑长命令时旁车可以安静好几分钟，然后突然写。
> **必须以「有没有 agent 在跑」为准。**
>
> ⚠️ **离线测试同样中招。** `npm test` 会拿真实旁车跑，在 dsh 外面拿不到 `ctx.agents`，
> 所以 `test.mjs` / `test-rewind.mjs` 用「会话日志近十分钟动过」当「可能在跑」，一律跳过。
> 那次事故有一半就是我一边改一边跑 `npm test` 打出来的。

**同类风险，已修**：`graft` 里的 `readSidecar` 读的是**父会话**的旁车，父会话正在跑时
开分支同样会打死它那一轮。本来以为要二选一——跳过 graft（新分支失忆）还是等父会话空下来
（可 `adoptBranch` 要求全同步）——其实不用选：**同步那一下照旧收手，另外排一个定时器
等它空下来再补**（`scheduleGraftRetry`，2 秒一探，最多 5 分钟）。

补做是安全的，因为 `graft` 自带 `child-already-bound` 那道闸：新分支一旦自己跑起来，
dsh-claude 会给它建旁车，补做那一下自动让位，不会覆盖任何东西。

> ⚠️ 定时器必须挂在 `apply` 那个 effect 的 dispose 上一起取消（`cancelPendingGrafts`），
> 否则它们会活过插件本身，在插件已经卸掉之后去写别人的旁车。test-lifecycle 用例 5 钉着。

⚠️ **别把旁车无脑 `JSON.parse`**：它的 `activities` 是整份对话原文，本机实测最大 6.7MB，
parse 一次 42ms，而 `/outlines` 每次都要过一遍全部会话。所以先在原文里找 `"ranges":[{`
这个串——非空的 `ranges` 必然长这样，**这一步只会少干活、不会漏判**（正文里凑巧有这串
就多解析一次，结论一样）。反过来"解析 `"ranges":` 后面那段"是不行的，那是在 6MB
对话正文里赌字符串位置。

⚠️ **盖戳必须盖在副本上**。传进去的 turns 是大纲缓存里那一份，就地改的话撤回状态
会被腌进缓存，之后 ranges 清空（新分支 graft 就会清）也刷不掉。

撤回掉的节点**不给 ＋ 按钮**：`planRewind` 把那几轮的锚点一起删了，从那儿开分支
只能开出一条没有上下文的失忆分支。详情卡上挂一个「撤回」小牌子，免得点开只看到
一条"怎么滚不过去"的旧提问。

#### 为什么不能换个渠道拿（三条路全试过了）

被打断的根源是"我们开着读句柄的时候，对面正好 rename"。实测（`D:/tmp` 下那个小脚本）：

| 我们做什么 | 对面的 rename |
|---|---|
| `statSync` / `existsSync` × 60 | **0 次失败** |
| 读句柄开着的时候 rename × 40 | **40 次全失败** EPERM |

所以判据很清楚：**元数据查询随便做，开读句柄要看人脸色。**

于是想过三条绕开的路，都不通：

1. **先复制一份再读**——复制本身就是一次读，句柄时长一样甚至更长。硬链接同理：
   Windows 上替换一个名字要对**文件对象**拿 DELETE 权限，另一个名字上的读句柄照样挡着。

2. **走 dsh-claude 自己的 `GET /plugins/dsh-claude/projection/<id>`**——它的 envelope 里
   确实有 `rewind.ranges`，`sidecar.read()` 也排在它自己的写队列里、零竞态。
   **但那条路由会掐断浏览器的实时转写流**：

   ```js
   // dsh-claude 的 StreamRegistry.admit
   live.get(key)?.()   // 同 key 的老连接当场被关掉
   ```

   路由注册时 `streamKey: () => MULTI_SEGMENT` 是个**常量**，单会话快照和 `/multi` 共用
   同一个 key；而浏览器唯一用的就是 `/multi`（那条就是 Claude 界面的实时转写流）。
   我们每 poll 一次就把它掐一次 —— 拿"偶尔打死一轮"换"持续掐断转写"，更糟。

3. **进程内直接问它要**——`const sidecar = new ClaudeSidecarRepository()` 是它 `apply`
   里的一个局部变量，没挂到 `ctx` 上，外面拿不到。

结论：**只能"在跑就不读"**。这不是懒，是穷尽之后的唯一解。

### 为什么需要 host 半

浏览器那边拿不到**冷分支**（fork 出来但没打开过的）的轮次。宿主的会话列表是这么写的：

```js
header.isSeeded ? undefined : cache?.cachedSnapshot(header, SessionLogOffset(0))
```

—— 对 seeded（= fork 出来的）会话直接返回 `undefined`。而 fork 出来的会话正是"分支"本身。
所以 host 半自己折一遍日志，用一条路由喂给前端：

```
GET /plugins/dsh-tree/outlines?cwd=<工作目录>
```

按 persistence 给的 `revision` 缓存，没变就不重读盘。

### 路由自己要设防 —— 宿主一点都不管

`@deepseek-ai/dsh-host-webserver` 的 README 写得很直白：

> **No server-wide TLS, authentication, or origin policy** — route owners such as
> `dsh-client-connection` enforce their own request policy.

也就是说 `webServer.register` 注册的路由**不继承任何鉴权，谁注册谁负责**。我们注册了三条，
`/outlines` 会吐出每一轮的提问预览，`/shape` 和 `/icon` 是写操作。而这台机器上
`remote-web-ui` 开着 `lanBind: true`、webserver 绑 `0.0.0.0` —— 不设防就是同网段可达。

#### 走过的弯路：自己写的那道闸不够

第一版是手写的两道检查，威胁模型只有两条：CSRF（Origin 必须同源 + 只收 JSON）和
DNS rebinding（Host 只认 IP 字面量，因为 rebinding 必须靠域名才能换掉解析结果）。
逻辑本身没错，**但它默认了攻击者是一个浏览器**。真实的局面里还有第三种，而且最省事：

> 同网段的人直接 `curl http://192.168.x.x:3080/plugins/dsh-tree/outlines`。
> 没有浏览器参与，Origin 和 Host 都随他填，而"放行所有 IP 字面量"正好给他开着门。
> 一次请求就能读走全部工作目录、全部会话的标题和每一轮的提问预览。

问题出在那条判据本身："它不可能是 rebinding" 只是**必要条件**，不是充分条件。
真正缺的那一半是鉴权，而鉴权不是路由层能自己糊出来的。

#### 现在的做法：把判决交给宿主

`route()` 里第一件事是问 `connection.requestRejection(req)` —— 那是 `/api/*` 走的
同一道闸，第一方插件 `@deepseek-ai/dsh-host-open-in-app` 的三条路由用的也是它。
返回 `undefined` 放行，否则就是该回的状态码。它做三件我们不该自己做的事：

| 它挡的 | 怎么挡 |
|---|---|
| **DNS rebinding** | Host 必须是 loopback，或 client-connection 的 `trustedHosts` 里声明过的部署地址 |
| **CSRF** | `sec-fetch-site: cross-site` 一律拒；带了 Origin 就必须同源（顺带把 `Origin: null` 按不同源处理 —— 手写那版是放行的） |
| **没登录的人** | 签名 cookie 对不上回 401。这是手写版**完全没有**的一层 |

我们自己只剩两条不属于鉴权的：POST 只收 `application/json`，以及 body **边收边数**
（上限 64 KiB）。以前是 `for await` 一路 push 到底再 parse，等于对面发多少就吃多少；
收完再判没有意义，内存那时已经吃进去了。

> ⚠️ **拿不到 `connection` 就回 503，不许 fail-open**。fail-open 等于整道围栏白写。
> `connection` 因此写进**顶层 `inject`**：没有它，这三条路由宁可不开。
> test-http 用例 7 钉着这条。

#### 手机怎么办 —— 这道闸是 loopback-only 的

宿主那道闸不认局域网 IP，所以局域网页面直连必然被拒。**这不是要绕过的障碍，
宿主给这种情况留了正门**：`remote-web-ui` 的 `/remote` 前缀。它做完配对校验，
再以 127.0.0.1 把请求重发进来，于是围栏自然过。那是个**通用前缀转发**
（`/remote/<任意路径>`），不限于它自己那几条路由，我们直接借用即可 —— 不碰它任何内部 API。

浏览器半（`src/client/net.js` 的 `send`）因此是这么写的：

1. 先直连。桌面端（127.0.0.1）就到此为止，**不会**白跑 `/remote`（那台机器上很可能压根没装）。
2. 只在 **401 / 403** 时改走 `/remote` 重试一次。404 / 500 是别的毛病，换条路一样错。
3. 换成了就把 `viaRemote` 钉住，此后不再多跑一个来回。
4. 换路也不通时，还回去的是**直连那份错**。`/remote` 的 404 只会让人以为路由写错了，
   而直连那份 401 说的才是真正的原因。

> ⚠️ 节点上的自定义图片是 CSS `background: url(...)`，**不走 fetch，也就没有重试的机会**。
> 所以 `shapes.js` 的 `iconUrl()` 要带上 `net.js` 试出来的前缀，否则手机上图会静静地空着。
> 时序上够用：图是 `/outlines` 回来之后才画的。
>
> ⚠️ `remote-web-ui` 的 fetch 补丁只给**它自己改写过**的请求加免 cookie 设备头，
> `/plugins/...` 不在它的名单里 —— 我们自己拼的 `/remote/...` 得自己从
> `sessionStorage['dsh-remote-device']` 取了带上。

这四条外加图片前缀，test-net 一条一条钉着 —— 它们的失败方式全是静默的。

---

## 4. 必须修的两个原生缺陷

两个都在 host 半的 `agent/created` 监听里处理 —— **分支一被造出来就接管**，
不管是谁造的（本插件的按钮、原生消息行上的分支按钮、将来的命令行），没有旁路。

> ⚠️ **不要把这件事挪到浏览器里做。** 试过包 `ctx.sessions.fork`，**一次都没生效过**；
> 而且原生那个分支按钮本来就绕过任何客户端包装。

### 缺陷一：fork 会多抄一条待办（所有 provider 都中招）

宿主的切法是"切在 `turn/end`，然后一直吃到下一个 `turn/start` 之前"。真实例子：

```
seq37  inbox 入队「加上44呢？」   ← 用户抢跑打的字，还没轮到它
seq38  turn/start turn3「加上22是多少」
seq45  turn/end   turn3
```

在 turn3 后面开岔路，继承前缀到 seq45，**seq37 那条入队也在里面**。新分支一激活先把
「加上44呢？」跑一遍，再跑用户新打的字。

这也是「点节点 N，分支却接在 N+1 后面」的真身：**每条提问都是在上一轮结束后才入队的**，
所以在 N 处开岔路必然继承到"N+1 的那条提问"。不是 UI 的 off-by-one。

**修法**：把继承段的 inbox 折一遍，拿到那几条的 id，逐个 `agent.inbox.remove(id)`。
盘上 21 个真实分支里有 13 个中招。

> ⚠️ **按 id 删，不要 `inbox.clear()`**：dsh 重启会把每个会话 resume 一遍、同样触发
> `agent/created`，那时队列里可能正躺着用户自己排的待办。
>
> ⚠️ **也不要加"只认刚出生的分支"之类的前置判断。** 踩过：判据用
> `session.isOwnSeq(session.inheritedEventCount)`，而 `session/end-seed` 恰好就写在
> `seq == inheritedEventCount` 上、创建那一刻就存在，于是判断恒为 true，
> **整个接管一次都没跑过**。按 id 删本身就是幂等的，不需要任何前置判断。

### 缺陷二：外部引擎的记忆没跟过去（只有 dsh-claude 这类中招）

正常接入的 provider（deepseek 等），对话原文就在 dsh 自己的日志里，
模型每轮的上文也是从这份日志重建的 —— fork 把日志前缀抄过去，新分支自然记得前文，
**插件一个字都不用管**。

`dsh-claude` 是例外：它不是 API provider，而是把本机的 Claude Code 当对话引擎。
日志里每条 `assistant/message` 的 `content` 都是空的：

```
deepseek-official  seq16 assistant/message  content=[{"type":"text","text":"在的，…"}]
claude             seq24 assistant/message  content=[]          ← 一个字都没有
```

真正的对话在 Claude Code 自己的会话里，dsh 只存一个指针（sidecar 的
`binding.claudeSessionId`）。指针没跟过去，新分支就拿到一个全新的空会话。

**修法**：用桥接自己的机制。给 SDK 传 `resume:<父会话>` + `resumeSessionAt:<那一轮的锚点>`，
SDK 会分叉出一个新的 Claude Code 会话而不是改写父会话（原生 `/rewind` 走的就是这条路）。
触发开关是 sidecar 里的 `rewind.pending = { resumeAt }`。所以接管时替新分支把 sidecar 写好：

```
binding         ← 抄父分支的（指向同一个 Claude Code 会话）
rewind.anchors  ← 裁到岔路那一轮
rewind.pending  ← { resumeAt: 那一轮的锚点 uuid }
activities      ← 裁到岔路那一轮（UI 里助手正文的唯一来源）
```

| 边界情况 | 处理 | reason |
|---|---|---|
| 岔路点落在继承段里（分支的分支） | 沿血缘上溯找持有该锚点的祖先 | —— |
| **一路上都没有 sidecar** | **什么都不做**，原生 fork 已经够了 | `native-context-is-enough` |
| 有 sidecar 但缺这一轮的锚点 | 什么都不做 | `no-anchor` |
| 新分支已经有 sidecar | 拒绝覆盖 | `child-already-bound` |
| dsh-claude 升了 sidecar 格式 | 停手（它的解析器遇到不认识的文档会 throw） | `native-context-is-enough` |

**依赖关系：没有。** 不 import dsh-claude，也不要求它装着，判据只有"那个文件在不在"。
最差的后果只是新分支失忆，**不会弄坏任何已有会话**。

> ⚠️ **半成品不许进大纲缓存**：分支刚建出来那一瞬间 `session/end-seed` 可能还没落盘，
> 这时折出来的大纲会把继承轮全当成"自有"。一旦被缓存，要等这条分支下次写日志才会刷掉。
> 判据：宿主说它 seeded，却找不到岔路点。

---

## 5. UI

### `dsh.client.inject` 要跟着挂载点走

`package.json` 里这张表决定浏览器半能用到谁，写漏了就是运行时才炸。当前五项各有出处：

| 声明 | 为什么要 |
|---|---|
| `dsh-api-session-controller` | `ctx.sessions`（列表 / open / fork / jump） |
| `dsh-api-workspace-controller` | `ctx.workspaces`（归档集） |
| `dsh-client-ui-layout` | **声明 `shell.overlay` 的就是它**，导轨挂在这 |
| `dsh-client-ui-chat` | 我们读它的 DOM：`[data-conversation-scroll]`、`[data-chat-turn]`、`TurnNavigator.module.css` |
| `dsh-client-ui-settings` | 设置卡片的 `settings.plugin.item` |

> ⚠️ 改挂载点时**必须同步改这里**。从 `conversation.session.header.utilities` 换到
> `shell.overlay` 那次，原本写的是 `dsh-client-ui-conversation` —— 那个包既不声明
> `shell.overlay`、也不拥有上面三个 DOM 锚点（它们全在 `ui-chat`），留着是纯误导。

改这张表**要重启 dsh**，光刷新浏览器不够。改完导轨要是没了，先把这里回滚。

### 挂在哪个 slot —— 决定了切会话会不会"闪"

导轨注册在 **`shell.overlay`**（`scope: "root"`，由 `AppFrame` 常驻渲染）。

> ⚠️ **不要**挂回 `conversation.session.header.utilities`。宿主把它声明成
> `scope: "session"`（见 `dsh-client-ui-conversation` 的 slot 注册），切会话时整个
> session 子树连同我们的组件一起卸载重挂：`box`、`activeTurn`、缓存的树全部清零，
> `outlines` 还要重新 fetch（带 120ms 防抖）。导轨会**真的消失再出现**，机器越卡越明显。
> 这不是"渲染慢"，靠保留上一帧数据救不了 —— ref 本身都跟着没了。

代价：导轨变成全局常驻，设置页/全局面板上也会渲染。用"量不量得到聊天区容器"当开关，
并加 `graceMs`(600ms) 宽限期区分两种消失：

| 聊天区容器 | 含义 | 处理 |
|---|---|---|
| 短暂消失（< 600ms） | 正在切会话 | 保留上次几何，什么都不动 |
| 持续消失（≥ 600ms） | 不在会话界面 | `box = undefined`，导轨收起 |

收起时组件**不卸载**，hover / 上一棵树都还在，切回来是瞬时的。

### 坐标

- **y = 树深度**，不是"第几行"。所以同一个岔路点分出去的两条支线，第一个节点**同高度**。
- **x = 列**，且**与"当前在哪条分支"无关**。同一会话的延续继承本列（主干天然是直线），
  岔出去的分支各领一个新列往左排。切分支只换颜色，**图的形状不动**。
- **省略**：只画离当前这一轮足够近的节点，放不下再压行高。"够近"有**两种量法**，
  由 `visibleMode` 二选一：`'depth'` 按层高差（默认，`visibleDepth`），
  `'step'` 按树上的**无向**步数（`visibleRadius`）—— 父节点 1 步，父节点的另一个孩子 2 步。

### 列距：按"画出来最宽的那个形状"留

`Z.lane`（17px）**是下限，不是列距本身**。它当年是按圆点定的，而形状是可配的：
收藏的五角星放大 1.67 倍（11px 的点画出来 18.4px）、四角星 1.81 倍（19.9px），
塞进 17px 的列里，相邻两列就左右贴脸 —— John 报的就是这条。

所以 `railLayout` 多收一个 `widestOf(dotSize)`：扫一遍**整棵树**，
算出画得最宽的那个形状占多少像素，列距取 `max(Z.lane, 最宽 + Z.laneGap)`。
第 0 列靠着导轨右缘，留的宽度同理取 `max(Z.hit, 最宽 + Z.laneGap)`，
否则星星的右半边会探到导轨外面。

- ⚠️ 扫**整棵树**而不是这一屏画出来的那几个。只扫可见的话，一颗星星滚进省略窗口、
  又滚出去，导轨就会跟着变宽变窄 —— 和 `maxColumn` 用整棵树是同一条理由。
- ⚠️ 横向占宽用 `drawnWidth` 不是 `shapeBox`。菱形是正方形转 45°，边长还是 `shapeBox`，
  **外接宽度是对角线**，多 41%。（`reachFor` 里那个 `size * 0.71` 是同一个事实的一半。）
- 命中区跟着 `max(Z.hit, lane)` 走：列被撑开时不跟着撑，两列之间会裂出一条点不中的缝。
  反过来列距比 `Z.hit` 窄时**不收窄** —— 命中区互相重叠是故意的。
- 全是圆点时这一整套算出来和改动前逐像素相同（test-layout.mjs 用例 1 钉着这条）。

### 横向落位：贴着右缘钉死；放不下就压列距

位置就是**贴着聊天区右缘**（`railRight`），一个像素都不挪。

> ⚠️ 中间试过"在空当里居中"，John 试完说还是靠右，撤回了。**别再改回去**：
> 居中会让树的横坐标随树宽（岔路数、有没有宽图标）浮动，眼睛每次得重新找一遍它在哪；
> 靠右是钉死的，扫一眼就知道往哪看。

空间不够**不是靠挪位置解决的，是靠压列距**——和行高那条"放不下就压"完全对称：

| | 竖着 | 横着 |
|---|---|---|
| 可用空间 | `box.height - pad*2` | `railRoom(box)` |
| 挤压对象 | `rowH` | `lane`（连带 `edge`） |
| 下限 | `Z.rowMin` | `max(Z.rowMin, dotSize+2)`（两个点还分得开） |
| 到底了还放不下 | 溢出 | 溢出 |

`railRoom` = 「聊天正文右缘 ～ 聊天区右缘」那条空当，两头各让 `Z.gap`。
正文右缘由 `contentRightOf` 量（取所有 `[data-chat-turn]` 里最靠右的那条）。

- ⚠️ 量不到正文右缘时**不能当作"随便长"**，要退回"到聊天区左缘为止"（`content = 0`）——
  那至少挡住了"从屏幕左边长出去"这一半。
- ⚠️ 压到底还是塞不下就**让它超出去**。一棵糊成一团的树比一棵探出去一截的树更没用。
- 列距本来就跟着最宽的形状长（上一节），岔路一多再挂几个宽图标，树能一路长到盖住正文 ——
  这条限位就是为那个场面准备的。
- 全都放得下时，这一整套算出来和不设限逐像素相同（test-layout 用例 2 钉着）。

### 别的插件盖在聊天上面时，树要跟着收起来

导轨是 `position: fixed` 的全局浮层，只认聊天容器的 `getBoundingClientRect()`。
别的插件（`better-sidebar` 这类）把侧栏**盖**在聊天上面时，聊天容器还老老实实待在原地、
尺寸一点没变 —— 于是屏幕上已经看不见一句对话了，却还有一棵树孤零零挂在那儿。

`isCovered` 不认任何具体插件，只问一句"**这块地方现在谁在最上面**"：
在聊天区里打四个点，`elementFromPoint` 回来的要么是聊天区自己（或它的子孙），
要么是它的祖先（＝点落在空白处，上面没人），**两样都不是**就说明有个兄弟子树压在上面。

- ⚠️ 必须**四个点全被盖住**才算盖住。只挑一个点的话，一个气泡提示、一个下拉菜单飘过去
  就会把整棵树闪掉。
- ⚠️ 导轨自己不算遮挡（最外层挂 `RAIL_MARK`，探针认得出来），否则会来回闪。
- 侧栏是"挤"而不是"盖"的那种（真把聊天区改窄了）不需要这条：容器 rect 跟着变小，
  `ResizeObserver` 那一路本来就跟得上，空当也跟着变窄，居中自然退回贴右缘。

### 省略（`elide`）

裁完要把 `depth` **重新压实**成连续的 `row`，否则藏了节点也腾不出地方，等于白做。
`rowOf` 就是干这个的；上限 = 0 时 `row === depth`，整套退化成老画法
（test-elide.mjs 用例 3 钉着这条）。

**两种量法，一个出口。** `elide(nodes, anchor, limit, mode)` 只管把"谁离多远"算成一张
`Map<node, away>`，淡出、压行、计数都共用后半段：
- `stepsAway`（`mode` 不是 `'depth'` 时）—— BFS 走 parent/children，树上的无向步数。
- `layersAway`（`'depth'`）—— `|node.depth − anchor.depth|`，**不看连通性**。

为什么要第二种：按步数量时，隔壁分支上和你**同一层**的节点得"爬回岔路口再下来"，
常常差二十几步。于是屏幕上明明并排的一横排，一个在一个不在，观感很怪
（用例 11 拿一棵同层差 20 步的树钉着这条）。按层数量就没这个问题 ——
一横排要么整排都在、要么整排都不在。所以默认是**按层数 10**。

⚠️ 两个上限**各存各的字段**（`visibleDepth` / `visibleRadius`），来回切量法不会把对方
的值冲掉。读哪一个只由 `visibleMode` 决定，兜底全都收在 `visibleRange()` 里 ——
rail 和设置卡共用这一个出口，免得两边各兜各的（用例 13 钉着）。

⚠️ 基准点是**正在看的那一轮**，所以滚动会让可见集变化、树跟着重排。这是功能本身决定的，
不是 bug；嫌晃就把范围调到"不省略"。

### 鱼眼淡出（`FADE` / `fisheye`）

边界不硬切：最外 `FADE.rings`（2）圈越画越小、越画越淡，到上限处正好消失。
`elide` 给每个留下来的节点算一个 `dimOf`（0 = 正常，往外每圈 +1），
`fisheye(level)` 把档位翻成 `{scale, alpha}`。

**以前是在剪断处画一个「⋯」，推翻了。** 两个毛病：`⋯` 是**文字**，和整张图的几何语言
（点＋线）不是一套，糊在一堆小圆点里很扎眼；而且它只说"这里断了"，不说断得有多软。
换成鱼眼之后边界处没有任何新元素，树自己淡下去。连带删掉的还有 `Z.ell`（那个字的字号）
和顶底各一行"放省略号"的空行。

⚠️ **淡出圈吃的是上限自己的最外层**，不是往外多画两圈。「12 步」说的就是最远画到 12 步：
第 10 步以内正常，第 11 / 12 步淡出；按层数那一档同理。写成 `limit + rings` 会让设置说谎，
还凭空多占两行 —— 导轨本来就在压行高（用例 4 拿独立 BFS、用例 12 拿独立层差钉着 ≤ limit）。

⚠️ 淡是**乘**在原有透明度上的。路径外的点本来就只有 0.4，直接赋值的话最外圈反而比
路径外的普通点更亮，**越远越显眼**，正好反了（用例 4b 钉着）。

⚠️ 上限比 `rings` 还小时（配置文件里手改出来的）要夹一层 `min(away, …)`，
不然连基准点自己都会被淡掉 —— 你正看着的那一轮必须永远是实的。

⚠️ 让开量（`reachFor`）要跟着 `scale` 缩。点画小了线不跟着多连一截，边界那两圈就是
"点和线各空一截"，看着像没接上。

滑上去把淡出撤掉，但**不改尺寸** —— `size` 决定 `left/top`，一变整个点就跳一下，
`transition` 只过渡 `transform`/`opacity`，拦不住这种位移。放大交给已有的 `scale(1.4)`。

代价说清楚：一棵正好长到第 12 步就到头的树，末端也会淡，看着像"后面还有"。换成
"只有真被砍了才淡"的话，同一行里会出现一个亮叶子挨着一个淡节点，更怪。按距离淡是鱼眼的
本义（远＝不重要），不是"外面还有东西"的信号。

### 设置

host 半用 `ctx.inject(['settings'], …)` 注册 namespace `dsh-tree`（**不能**写进顶层
`inject`——没挂设置提供方的部署会让整个 fiber 永远 pending）。浏览器半用
`ctx.settingsScope.bind({namespace:'dsh-tree'})` 读写，并把卡片注册到 `settings.plugin.item`。

宿主只在「host 服务了这个 namespace」**且**「有卡片认领它」时才渲染，所以 host 半没重启时
卡片整个不出现，前端自动退回默认的「按层数 10」—— 这也是为什么改完 `index.js` 必须重启 dsh。
**反过来说：卡片只要出现了，就证明 host 注册成功了**，这时候再出问题一定在前端。

卡片外观照抄 `ui-settings-plugins` 的 `PluginCard` / `fields`：外层 `<li>`（宿主铺的是
`<ul>`）、16px 圆角、点标题行展开、14px 折角箭头转 180°。颜色一律走 `--dsw-alias-*`
变量而不是写死色号，换主题自动跟随。

⚠️ `writable` 要**逐帧**判断。第一帧几乎必然是 `status:'loading'` + `writable:false`，
当时若把 `set` 删掉就再也加不回来，滑杆永远是灰的（踩过，test-elide.mjs 用例 8 钉着）。

**基准尺寸调过一次。** `Z` 里被 `scaleZ` 缩放的那八项是老基准的 1.2 倍再取整 —— 老基准
要手动调到 120% 才顺眼，那就把 120% 挪成默认的 100%。取整是为了 1px 描边落在整像素上，
代价是各项相对老基准差 ±2% 以内（test-elide 用例 10 钉着）。改 `Z.dot` 时记得回头看一眼
`ICON_EDGE` —— 上传的图存多大是按最大能画到多大算的。

设置项写在 `FIELDS` 表里，卡片按表渲染、store 按表取值 —— 加一项只改这张表和 host 的
schema，别再去动卡片。**字段名两边必须一模一样**。

### 缩放（`scaleZ`）

`Z` 里的几何量按百分比整体缩放：`dot` / `lane` / `hit` / `row`。
`restMs`（时间）和 `card`（详情卡宽度）**不缩** —— 卡片跟着放大只会挡住聊天区。

关键不变式：`scaleZ(100)` 必须与 `Z` **逐字段相等**，否则"默认值"会悄悄改掉现有画法
（用例 10 钉着）。另外 `hit / lane` 的比值必须恒定 —— 命中区比列距宽 4px，
只缩一个会让相邻列的命中区互相吞掉。

描边宽度和外发光在 `dotStyle` 里按 `size / Z.dot` 同比走，不然点放大后边框细得看不见。

### 节点形态：形状归 kind，状态归 active/focused

两者**正交**，`dotStyle(kind, active, hover, size, focused)` 一次性算出所有字段。

| | 默认长相 | 谁决定 |
|---|---|---|
| 普通轮次 | 正圆 | `normalShape` |
| 在当前路径上 | 正圆 + 边框变亮、填充淡色、不透明 | `currentShape` + `active` |
| **压缩轮次** | **倒三角**（尖朝下）+ 橙色 | `compactShape` |
| 树根空节点 | 虚线（形状同普通轮次） | `kind` |
| 正看着这一轮 | 实心填充 + 外发光 | `focused` |

三个角色的形状**各自独立**：`shapeOf(kind, active, theme)` 先看是不是压缩，再看在不在
当前路径上。

> ⚠️ 别再把 current 和 normal 合成一个。合了之后设置里"当前路径形状"怎么改都没反应
> —— John 报的"改了好像没反应"就是这条（用例 12 钉着，合回去炸 6 条）。

> ⚠️ 别再写 `kind = focused ? 'current' : node.kind`。那样压缩节点一滑到就变回蓝圆点，
> "一眼看出是压缩节点"恰好在最该看清的时候失效（test-highlight 用例 5 钉着）。

**方框画不出来的形状，由 `<span>` 里面的东西画。** 三角是 `<svg><polygon>`，自定义字就是
那个字，上传的图走 `background-image`。这三类一律 `borderWidth: 0` + `background: none`
—— 方框留着就是"框里画了个三角"。外发光也得换：`box-shadow` 画的是**方框**的光晕，
套在三角外面是个方的光，所以改用跟着轮廓走的 `filter: drop-shadow(...)`。

> ⚠️ 三角**别用 `clip-path`**。剪出来的东西斜边上没有描边（border 被一起剪掉），
> 只能整块填实 —— 摆在一排空心圆里一眼就看得出是两拨人画的（John 报的"质感明显
> 和其他图案不一样"）。

> ⚠️ polygon 的 `fill` / `stroke` / `strokeWidth` 必须走 `inkOf()` 和方框类**同一套**。
> 各算各的必然对不齐。这组属性抽在 `polyProps()` 里就是为了能测 —— 藏在渲染函数里
> 的话，把 `fill` 改成 `skin.ink`（整块填实）一条断言都不会响。

**大小按面积配齐，不是按边长。** 底 1 高 0.87 的三角只占单位框的 0.435，而正圆占
π/4 ≈ 0.785，所以 `grow = √(0.785/0.435) ≈ 1.34`。`shapeBox()` 算实际画多大，
`reachFor()` 也得跟着它走，否则放大后的尖角会戳到连线上。

压缩标记来自 host 半：`compaction/end` 的 `data.turn`。

> ⚠️ **压缩失败也会发 `compaction/end`**，只是带 `error`（宿主校验器：成功的 end 必须
> 配一条 `compaction/summary`）。不看 `error` 就会把没压成的轮次画成菱形。

**桥接兑底**：`dsh-claude` 这类桥接里，`/compact` 不会被 dsh 的命令分发拦下，
而是当普通提示词发给外部引擎，压缩全程在引擎内部完成 —— dsh 日志里一条
`compaction/*` 都没有（盘上 64 个会话实测为 0）。所以还认一条：**本轮提示词以
`/compact` 开头**。两条判据并存，原生 provider 走 `compaction/end` 那条。

### 高亮：边框归路径，填充归滚动

```
边框蓝  ⟺  这个节点在当前会话的对话里
填充蓝  ⟺  边框已经是蓝的  且  轮次号 == 现在滑到的那一轮
```

落成代码是给血缘链上每个会话记一个"轮次上限"，从当前会话往祖先走，
上限取**一路上岔路点的最小值**（A→B→C→D 时，D 只继承 C 的前 2 轮而 C 继承 B 的前 3 轮，
那么 B 的第 3 轮不在 D 的对话里 —— 只看相邻一层会多算）。

> ⚠️ **别改回"从末端节点沿 `node.parent` 往上爬"**：一条分支的岔路点若落在父分支继承来的
> 那一段里，挂载点会退回好几层，沿 parent 走就跳过中间节点 —— 屏幕上是"稀稀疏疏几个蓝点"。
>
> ⚠️ **填充那条不许加"节点属于当前会话"**：继承来的那几轮画在图上的是**父会话的节点**，
> 加了这个条件，往上滑到继承段就一个点都不亮。
>
> ⚠️ **节点样式的 key 集合必须在所有形态间一致，边框只用 longhand。**
> 踩过一次，症状极具迷惑性："滑过一个点它就白一个，越滑越花"，看着像高亮算错了，
> 其实是 React 的内联样式 diff —— 它会把**上一帧有、这一帧没有**的属性置空。
> base 写 `border: '1.5px solid 蓝'`（简写）、"当前轮"那一支额外写 `borderColor`，
> 切回来时 `borderColor` 被清成 `''`，border-color 退回 `currentColor` → 白边框，
> 而 `border` 简写字符串没变、React 不会重写回去。

### 交互

| 操作 | 结果 |
|---|---|
| 单击节点 | 跳到那一轮 |
| 悬停节点 | 右侧滑出详情卡（**收起档**：名字 / ＋ / ☆） |
| 在卡片上双击 | 展开档：名字进文本框，⇤ / ⇥ / ⊕ 露面 |
| ＋ | 从这之后接着问（见下） |
| ☆ | 收藏 —— 这个点在树上变成黄色五角星（见下） |
| ⇥ | 把这条支线拆成独立的一棵树（见下） |

**点击：尽量不换路径。** 点的节点如果就在当前路径上，**留在当前会话里滚过去**，
不要切到"这个节点所属的那个会话"。能这么干是因为 fork 把父分支日志原样抄了一份、
**seq 一个都没变**，所以同一个 seq 在当前会话里指的就是同一轮。

> 不这么干的后果：路径是 1-2-6-7-8 时点一下节点 2 → 切到父会话 → 高亮整条换成 1-2-3-4-5。

**＋ 什么时候才真的 fork：**

| 点的是 | 动作 | 为什么 |
|---|---|---|
| 树根空节点，**底下还没有分支** | **什么都不做**（＋ 直接不画） | 刚建的对话本身就是那条空对话，再开一条只是复制粘贴 |
| 树根空节点，底下已有分支 | 新建对话 | 这才谈得上"再开一条" |
| **叶子节点**（后面既无后续轮、也无别的分支） | **就在本会话往下问，不 fork** | 复制一份只会在左边多出一条内容完全重复的会话 |
| 有后续的节点 | 真的 fork | 这才是要岔开 |

新建对话时**必须传 `workspaceId`，不能只传 `cwd`**。

> ⚠️ 侧栏按 `workspace.sessionIds` 这张显式成员表分组，和 cwd 无关。只传 cwd 建出来的
> 会话不在任何成员表里，于是掉进"未分组"分组。宿主自己的新建按钮就是
> `sessions.create({ workspaceId })`。查不到归属时才退回 cwd。

### 详情卡分两档

- **收起档**（鼠标停上去就是这档）：一行，信息 + **＋ 和 ☆，就这两个动作**。
- **展开档**（在卡片上双击）：第一行左边多出改树形那三颗（⇤ / ⇥ / ⊕），
  底下多出名字框和收藏图标那排。

**全部动作按钮都在第一行**，包括只有展开才露面的那三颗。

> 改树形那三颗本来整个收在展开档的**第二行**，和名字框挤在一起 —— 那是把"改结构"
> 和"改标注"两回事摞成一摞。现在按钮归第一行、输入归下面，两档之间只差"多不多三颗"。

> ⚠️ 它们**不在收起档露面**。收起档是鼠标划过导轨时跟着走的那一档，手还在动、
> 按钮又小又挨着，这会儿摆出"把这条支线拆出去"这种改结构的动作，迟早点错。
> 展开是一次明确的双击，相当于"我确实要动这个节点"。
> 收起时合并清单也一并收掉 —— 否则 ⊕ 没了，单子还摊在那儿。

**卡片里不写操作说明。** "再双击一次收起""单击上面的框就能改名""名字改过了"
这三句都删了 —— 双击、单击、框变蓝都是一眼就懂的事，写出来只占地方。

**改名：单击文本框就能改，改完只有「保存」和「不保存」两条出路。**
改过之后所有动作按钮一起灰掉：它们每一个都会触发重画，而重画就意味着草稿悄悄没了。

#### 框里有光标，整张卡就得按住

原来的锁只认"名字改过了"（`dirty`）。于是从**点进框**到**敲出第一个不一样的字**
之间有一段没上锁的真空期：鼠标稍微飘出卡片 → `onMouseLeave` → 280ms 后整张卡关掉 →
输入框跟着卸载 → 焦点掉回宿主那个 Lexical 编辑器 → **后面敲的字全进了聊天框**。
John 报的"输着输着就输到聊天框里去了"就是这条。

判据改成 `keepsCard(dirty, typing)` = 「框里有光标 **或** 改过了」。
"删回原样"那一下同理：`dirty` 翻回 false，可光标还在框里，仍要按住。
test-pointer 用例 7 钉着这两档。

- ⚠️ **按住**和**灰掉按钮**是两件事，别合并。按住只要框里有光标就生效（防的是卡片
  自己消失）；灰掉仍然只看 `dirty`（防的是丢草稿）。光点进框还没敲字时没东西可丢，
  这会儿把 ＋ 灰掉纯属碍事。
- 两个输入框（名字框、收藏图标的「字」框）都是**两行那么高**（`48px`），卡片一展开就是
  这个高度、不随内容忽高忽低。名字常常比框长，矮框里改字要一路盲敲；框越大，
  光标在里面时鼠标也越不容易蹭出卡片。

#### 关卡片之前，回到现场再问一句

`mouseleave` **说了不算**。

症状：卡片展开着，点一下"取消收藏"，卡片自己收起来了 —— 有时收有时不收（John 报的）。

原因是那一下会**改版式**：收藏图标那排整个消失、导轨宽度跟着变，而卡片是
`translateY(-50%)` 竖直居中的，变矮就意味着内容在鼠标底下挪走了。
**鼠标一动没动**，浏览器照样派一个 `mouseleave` 过来，老写法当场起表、280ms 后关掉。
改图标、改颜色、拆支线、合并 —— 每一个"点了自己会改版式"的动作都会踩到。

所以 `release()` 的计时器到点之后**先复核一次**（`overRail`）：那个坐标上最上面的元素
还是不是导轨的子孙？

- 还在 → **自己续一次表**，接着等。这样鼠标真走的时候照样关得掉，不会因为漏了一次
  `mouseleave` 就永远挂在那儿。
- 不在 → 关。

三条细节：
- ⚠️ 复核必须**等到计时器到点**再做。点击那一帧版式还没落定，当场量到的是旧的。
- ⚠️ 判据是 **DOM 包含关系**，不是矩形命中。卡片浮在导轨框的左边、几何上在框外，
  但它是导轨的子孙 —— 按矩形算的话，鼠标一挪到卡片上就被判成"走了"。
- ⚠️ 退化情形（没有导轨元素 / 还没收到过 mousemove / 那个坐标上什么都没有）一律
  **"该关就关"**。反过来会让卡片在某些情况下永远关不掉，那比误关更难受。

#### 焦点被宿主抢走就抢回来

"按住卡片"只解决了"卡片自己消失"。还剩两个症状：**打字打到一半跑进聊天框**、
**Ctrl+A 想全选节点描述结果全选了聊天区**。两个是同一件事——焦点被别人悄悄拿走了。
宿主什么时候重挂组件、Lexical 什么时候取回焦点，我们拦不住。

拦不住就抢回来（`useFocusGuard` + `shouldRefocus`），但**只抢"没有理由"的那一次**：

| 局面 | 抢吗 |
|---|---|
| 谁都没动，焦点自己没的 | **抢** ← 整条机制存在的理由 |
| 最近 `LEAVE_MS`(350ms) 内有 pointerdown / wheel | 不抢（用户真的想走） |
| 按了 Tab / Esc / 回车 | 不抢（同上） |
| 焦点落在卡片里别的东西上 | 不抢（自己人） |
| 这个框已经不想要焦点了（提交 / 卸载） | 不抢 |

- ⚠️ 少掉"用户自己点了别处"那条就变成**焦点陷阱**：点哪儿都弹回这个框，连关卡片都做不到，
  比原来的 bug 更糟。test-pointer 用例 9 有三条专门盯着这个方向。
- ⚠️ 抢回焦点必须排到**下一帧**（`setTimeout 0`）。`blur` 派发的当口 `document.activeElement`
  还没落定，当场 `focus()` 会被紧跟着那一手再抢走，两边来回弹。
- ⚠️ `focus({ preventScroll: true })`。不加的话抢回焦点会把聊天区滚到导轨那一行去。
- 另一半是**把键盘事件拦在自己家里**：两个框的 `onKeyDown/onKeyUp/onKeyPress` 一律
  第一时间 `stopPropagation()`。宿主在上层挂着自己的快捷键，不拦的话框里敲的每一下
  它都收得到 —— Ctrl+A 全选聊天就是这么来的（用例 10 钉着）。

#### React + 输入法：业界公认的那四条

这四条是 React 社区打磨了很多年的定式（facebook/react#3926 / #8683 那串 issue，
antd 的 rc-input、Slate、CodeMirror 的 React 封装用的都是同一套）。我们四条全在。

1. **`compositionstart` / `compositionend` 记一个标志**，再叠一层 `nativeEvent.isComposing`
   —— 两条都要：`isComposing` 是标准写法，但 Safari 和几个国产输入法只给 composition 事件。
2. **受控的 `value` 每一次 `input` 都要跟上，拼字中途也不例外。** React 只在
   `props.value !== node.value` 时才去写 DOM，而**写 DOM 就会掐掉正在进行的合成**。
   所以中途"不 setState"反而是错的 —— 那会让 props 落后于 DOM，下一次任何重渲染
   都会把拼到一半的字冲掉。要做的是：中途照常更新自己的草稿，**只是不往上游提交**。
3. **拼字中途一个按键都不许动。** ← 最容易漏、代价最大的一条。
4. **拼字期间不许换 `key` / 卸载重挂**那个输入框。

> ⚠️ 第 3 条是 John 连着报了两次的那个。中文输入法**用空格或回车选词**，那一下会先派发
> 一个 `keydown{key:'Enter', isComposing:true}`。任何在这上面动手的代码（保存、关卡片、
> `blur()`）都会让浏览器当场掐掉这次合成，把**拼音原文**当结果落进框里 ——
> 打完 "ceshiyixia" 还没选词，框里就成了那串英文。Esc 同理：它在输入法里是
> "关掉候选词"，不是"取消"。
>
> 名字框一开始就有这道闸；收藏图标的「字」框后加的时候漏了，于是同一个 bug 原样重演了一遍。
> test-pointer 用例 11 现在钉的是**顺序**：`onKeyDown` 里问"是不是在拼字"这句，
> 必须排在任何一个 `event.key === 'Enter'/'Escape'/'Tab'` 前面。

> ⚠️ **中文输入法的坑（John 报过）**：老写法是双击直接进输入框、`keydown` 见到
> `Enter` 就"确认改名并收工"。但微软拼音选词按的就是**空格或回车**，那一下会先派发
> 一个 `keydown{key:'Enter', isComposing:true}` —— 于是拼音才打两个字母，卡片就自己
> 关了。Esc 同理：它在输入法里是"关掉候选词"，不是"放弃改名"。
> 判据是 `isComposingKey()`：`event.nativeEvent.isComposing` 为准，再用
> `compositionstart/end` 自己兜一层（Safari 和几个国产输入法不给那个标志）。
> test-highlight 用例 22 钉着这条。

### 收藏：黄色五角星

收藏和「角色」（普通 / 当前 / 压缩 / 空）**正交** —— 它不是第五种节点，而是盖在任何
一种节点上的一层记号。所以它既不进 `ROLES` 也不进 `THEME`：进了 `THEME` 就得在设置卡
里给它配颜色和形状，而"收藏"的意思本来就钉死在**黄色五角星**这五个字上。
（顺带保住了用例 15 那条「`THEME` 的每个键都要在设置里露面」的不变式。）

三条规矩：

* **平时空心，走到它那一轮才填实。** 一屏里收藏七八个点也不会糊成一片黄，
  真正滚到哪一个，哪一个才亮起来。
* **不跟着"路径外"那一档淡**（0.4 → 0.85）。收藏的意思就是"待会儿我要回来找它"，
  而它多半不在当前路径上，淡到看不见就白收藏了。
* **点下去有动画**，收藏和取消**各一条关键帧**（转出来 / 缩回去），不是把同一条倒放。
  非用 `@keyframes` 不可：收藏会把这个点**整个换一种形状**（圆 → 星），
  而 `transition` 只能在同一个属性的两个值之间过渡，换形状那一下补不出任何东西。

星星的顶点是**算出来的**（`starPoly()`），不是抄一串小数：改成六角星只要把参数从 5
换成 6，而手抄的坐标错一个小数点是看不出来的。`grow ≈ 1.67` 和别的多边形同一条
规矩 —— 交给 `growOf` 按**面积**和圆配齐，否则五个角把面积摊开，星星看着比旁边的圆小一圈。

#### 图标可换，颜色不可换

黄色五角星是**默认**，不是唯一。详情卡展开之后，收藏过的点会多出一排图标可选
（`FavIconRow`），词汇和设置卡里那排完全一样：12 个预设形状 / `char:<字>`（emoji 也行）
/ `img:<id>`（自己传的图）—— 所以图标库实际上是无限的。

#### 配色：收藏没有自己的规则，它只是换个色号

收藏色和另外四个角色色一样，在 `PALETTE` 里**分明暗两版**，上色走同一个 `paint`。

> **走过两段弯路，都记在这儿。**
>
> **第一段**：这里曾是"星身填亮黄 + 描边另算一个压深的同色"（`starInkOf`），理由是
> "同一个色值既要像黄又要扛对比度，在白底上是互斥的，那就拆开"。拆是拆开了，代价是
> **同一个 `#ffd43b` 能画出三个颜色**：普通节点"空心 + 黄边"，收藏"实心亮黄 + 暗黄边"。
> 用户设的只有一个色号，屏幕上哪个都对不上。
>
> **第二段**：改成"上屏前统一过一遍 `readable`"。范式是统一了，但它**改的是用户亲手
> 挑的那个色** —— John 在设置里挑了 `#fff833`，拿取色器在树上量到 `#9e9800`，
> 当场发现设置卡和屏幕上是两个色号。这违反了 `themeFrom` 早就立下的规矩：
> **没改过的跟着明暗走，改过的钉死。**
>
> **现在**：明暗自适应只发生在**默认值**这一层（`PALETTE` 两版），画的那一步
> 一个像素都不动。亮色那版由 `readable(STAR_COLOR, false)` **算出来**而不是手抄，
> 所以改了基色它自动跟着变。

| | 暗色 | 亮色 |
|---|---|---|
| 默认收藏色 | `#ffd43b`（对深底 13.3:1） | `#b88f00`（算出来的，3.01:1） |
| 用户挑的色 | 原样 | 原样 |

- ⚠️ **默认色必须自己扛住对比度**，画的时候不会再有人替它兜底。亮色的压缩色
  （宿主 amber-600）原值只有 2.79:1，差一点点，所以也过了一次 `readable`。
- ⚠️ `readable` **只动明度**，色相饱和度一个不碰（钉着色相偏移 < 2°、饱和度 < 0.06）。
- ⚠️ 算颜色那一整块**必须排在 `PALETTE` 前面**：亮色版收藏色是在模块初始化时算的，
  而 `BACKDROP` / `CONTRAST_MIN` 是 const —— 排在后面就是 TDZ，整个 bundle 加载时
  ReferenceError。踩过一次，好在炸得很响。

> ⚠️ **色板第一格是「恢复默认」，它必须把默认那个色本身画出来。** 以前是个空心虚线圈
> 加个 `×`，于是一排七个格子里**看不到默认的黄** —— "颜色选项里竟然没有默认收藏的那个
> 黄色"。虚线边继续留着当"这格是恢复默认"的记号，颜色照画（`favSwatch`，抽成纯函数
> 是为了能测）。

> ⚠️ **收藏不再自带实心。** 原来收藏一律填实，于是"填实"同时背着两个含义
> （已收藏 / 正看着这一轮），谁都读不出来是哪个；而且选择器里画的是半透明、
> 树上画的是实心，同一个东西两副样子。现在实心**只表示"正看着这一轮"**，
> 收藏靠形状（五角星）和颜色表达自己 —— 外加它不跟着"路径外"那一档淡下去（0.85 而不是 0.4）。

#### 卡片上那排选择器：形状和颜色挤在同一行

`FavIconRow` 现在是**一个** `flex-wrap`，形状、「字」框、传图、颜色全在里面，挤不下自己换行。
原来是"图标一排 + 颜色一排"，光两个标题就占掉两行，而卡片总共才十几行高。

省下来的行数来自四处，缺一条都回不到两行：

| 改动 | 省了什么 |
|---|---|
| 两个标题（「收藏图标」「收藏颜色」）删掉 | 圆的是颜色、方的是形状，一眼分得开，不需要字 |
| 格子 22px → `PICK`(18px)，间距 4 → `GAP`(3) | 每格省 5px |
| 「字」框从两行高回到和格子一样高 | **它一个人撑起了整排** —— 而它最多放 5 个字 |
| 卡片上不列右箭头 / 五边形 / 六边形 | 这三个在 11px 上和圆几乎没差别，占着格子不提供区分度 |

- ⚠️ 设置卡那边**不删**这三个形状。那儿是给节点配形状的，格子宽松；
  而且从 `SHAPES` 里删掉的话，已经存了 `hexagon` 的设置读出来就不合法了。
  所以卡片用的是 `FAV_SHAPES = ['star', ...SHAPES 去掉 FAV_DROP]`。
- ⚠️ 需要两行高的是**名字框**（那儿才写长句子），不是「字」框。别再把它撑高。
- 20 格 + 一个输入框在 270px 的卡片里排成 **2 行**（约 42px 高，改动前约 100px）。
  真要压成**字面意义上的一行**，只剩两条路：形状砍到 6 个以内，或者把卡片加宽到 450px 以上。

#### 默认样子在设置里可改

设置卡最后多一行「收藏」：**默认颜色 + 默认图标**（`favoriteColor` / `favoriteShape`）。
和四个角色那几行同一套控件，只是形状选择器前面多一格五角星。

- ⚠️ 收藏**不是第五个角色**（它是盖在任何一种节点上的记号，不在 `ROLES` 里），
  所以 `ROWS` 那一条的 `color` / `shape` 字段名是直接写出来的，不从 `ROLES` 查。
- ⚠️ 形状的校验走 `isFavShape` 不走 `isShape`：`shapeSpec('star')` **认不得**五角星
  （它故意不在 `SHAPES` 里），用 `isShape` 的话"默认形状 = star"存进去读出来就不合法，
  设置里改了跟没改一样。
- 这两项**不分明暗**（一个色值就够，明暗差别由 `readable` 在上屏前统一处理），
  所以 `favoriteColor` 放在 `SHAPE_DEFAULTS` 里而不是 `PALETTE` 里。
- 单个点在卡片里挑过图标 / 颜色的，仍按它自己的来 —— 设置改的是**默认**。
- 描边色**不给配**，它是算出来的。给配等于把刚拆开的两件事又按回一起。

**颜色默认恒为那个黄，但可以按点改**（`FavIconRow` 底下那排色点 + 一个取色盘）。
那一排**不写标题**：一串圆色点加一个取色盘，看一眼就知道是什么。

> ⚠️ 这条推翻了原来"只给形状，不给颜色"的硬规矩。当初的理由**现在仍然成立** ——
> 颜色一旦到处都是，"哪个是收藏"这件一眼能扫出来的事就得改成逐个辨认。
> 所以：**默认还是黄的**，字典里只躺用户显式改过的那几个（`nextFavColors` 里
> "恢复默认 = 删掉这一条"和图标那条是同一个道理）。真要按点分色
> （红=待办、绿=已验证）给得出，代价自己心里有数。

> ⚠️ 这个字符串**直接进 CSS**，所以 `starSkin` 只认 `#rrggbb`，别的一律退回默认。
> 认宽了就是一个注入口子（test-highlight 里有一串 `url(javascript:1)` 之类的反例钉着）。

> 卡片上那颗 ☆ 用**这个点自己的**颜色，不是全局那个黄 ——
> 不然改完颜色树上变了、卡片上没变，看着像没生效。

为什么角色形状只能在设置里改、收藏图标却在卡片里改：角色形状是"这一类节点长什么样"，
全局一份；收藏是**按点**贴的记号，一屏里收藏七八个点全长一个样等于没标。
它天然是每个点自己的事，也就只能在这个点自己的卡片里改。

#### 自定义字：上限 5 个码点，一份数说了算

`GLYPH_MAX = 5`，**输入框和渲染共用这一个常量**。

> ⚠️ 以前是两份：输入框写 `maxLength: 4`，`shapeSpec` 只认 2 个。于是打到第 3 个字时，
> 框里明明有字、树上的图标却悄悄退回了默认 —— "能输入，但不生效"是最难查的那种
> （John 报的）。现在超过 5 个直接打不进去，看得见的和画出来的永远是同一回事。

**裁按码点数，不按 `.length`。** `'🙂'.length` 是 2，按它算一个 emoji 就吃掉两格配额。

**字是横着摊开的**，所以宽高不对称：
- `drawnWidth` 跟着字数长（`grow = min(字数, GLYPH_SPAN=3)`），列距自动跟着撑开。
  封顶在 3 倍是因为列距按整棵树最宽的形状留 —— 不封的话，某一个节点挂个 5 字标签，
  **整棵树**的列距都被它撑开。
- `shapeHeight` 恒等于一个字高。连线让位按它算，否则挂了 3 字标签的点上下会凭空空出两倍的缝。
- 字号 `glyphFont` 按汉字宽度算（一个字一个 em）：1~3 个字用满字号，4~5 个字跟着缩，
  正好填满那个封顶的宽度。拉丁字母比这窄，算宽一点只会显得小一号，算窄了会糊出框外。

⚠️ 字**不能**直接当那个 `<span>` 的文本内容返回。它是个 size×size 的方框，两个字以上
就从右边糊出去（不是居中溢出）。和多边形一样绝对居中，往两边等量溢出。

#### 「字」框：中文输入法的第二个坑

老写法每敲一下就 `onPick` → 写 localStorage → **整条导轨重画** → 值再绕回框里。
那一圈落在拼字中途，拼一半的候选被当场冲掉 —— John 报的"自定义图标里一输中文就退出"。

三道闸，test-pointer 用例 8 逐条钉着：

1. **不挂 `maxLength`。** 中文是先把拼音打进框里再换成汉字的，"zhongguo" 八个字符才
   换来两个字；挂上 5 的上限，拼音打到第六个字母就被截断，汉字根本拼不出来。
   上限改成在拼字**落地之后**裁（`clampGlyph`）。
2. **框自己拿草稿**，`compositionstart/end` 决定什么时候落地。拼音期间只改草稿，
   一个字节都不写存储，导轨也就不会重画。
3. **框里有光标就把整张卡按住**（`onHold` → `keepsCard`），和名字框同一条规矩。

解析走 `favShape()` 而不是 `shapeSpec()`：

> ⚠️ `shapeSpec` 认不得的值退回的是**圆**，而收藏的默认是**星**。收藏那条路要是
> 直接用 `shapeSpec`，手改一个字就能让一屏收藏全变成普通圆点。
> 另外 `'star'` 故意**不在 `SHAPES` 里** —— 进了就会出现在四个角色的形状选择器里，
> "哪个是收藏"同样当场失效。

存盘在 `dsh-tree.favicons`，和收藏清单**分开**（一个是集合、一个是字典，混着迟早判类型）。
**取消收藏时故意不删图标**：取消再收藏回来还是上次那个，不用重挑一遍。
而"恢复默认"是**删掉那一条**，不是存一个 `'star'` —— 存进去的话，哪天默认记号换了样子，
所有"没改过"的点会被陈年记录钉在旧样子上。

连线的让位量也得跟着走：挑了十字（`grow` 1.11）却按五角星（1.67）让位，
线会在点外面凭空断一截。所以 `reachFor` 的最后一个参数既认 `true`（默认星），
也认一个算好的 `shapeSpec`。

收藏清单和改名一样存 localStorage（`dsh-tree.favorites`），存的是**字符串数组**
而不是 `{key: true}`：它本来就是个集合，存成字典早晚有人写出
`favorites[key] === false` 这种"取消收藏"的假动作，于是清单里躺满取消过的键。

> 和改名共一个半成品的账：换浏览器就没了，也进不了手机。真正的落点是 host 半的
> `shape.json` 旁边，接口保持成 `readFavorites()/writeFavorite()` 两个函数，
> 就是为了那天只改 `labels.js` 一个文件。

### 没有"重做"按钮

**"重做第 N 轮"和"在第 N-1 个节点上按 ＋"是同一件事** —— 都是
`sessions.fork({sessionId, atSeq})`，差别只是前者顺手把原话填回输入框。而聊天区
每条消息旁边本来就有宿主自己的分叉入口（`forkAt`），再放一个只会多一条出错的路。

> ⚠️ 撤掉之前那个 ↺ 是有实账的：它按 `own[at-1]` 找上一轮，只看**自有**轮次，
> 所以在分支的第一个自有轮次上会落空，退化成"新建对话"；后来 `api.fresh` 改了签名
> （加了 workspaceId），这个退化路径还在按老签名调，把 cwd 当成 workspaceId 传了进去。

撤掉之后，树的正确性**完全依赖**于能否接住宿主发起的 fork。这条由 test.mjs 断言 11
盯着：岔路点与继承段必须自洽、自有轮次紧接岔路点、父分支一轮不少。

顺带澄清一个容易误会的地方：**dsh 的 fork 是复制不是截断**。在第 3 轮"重做"之后，
原来的 3/4/5 一轮都不会消失，它们留在原分支上，新分支是它的兄弟 —— 这正是树要画的东西。

### 改完树形要立刻看得见

`useOutlines` 的重拉条件里只有 `cwd` 和会话列表，而**改树形（分组/分离）这两样都不动**。

> ⚠️ 所以必须带一个 `nonce` 手动催拉，否则分离要等到下次发消息或切会话才顺带刷出来 ——
> 用起来就是"点了没反应，过几秒突然全生效"，用户会在空窗期反复点。这跟机器快慢无关。

两层：`reshape()` 先把 host 回的新形状塞进本地回显（点下去立刻见效），再 bump `nonce`
拉一次权威数据；服务端答复一到就清掉回显让位给它。

重复点是安全的：host 那边 `detached` 是个集合，同一个节点加两次等于加一次。而且剪完之后
那条链上再没有岔路，`canDetach` 自动变 false，按钮直接消失。

### 哪几条对话算同一棵树（`shape.json`）

dsh 只记 fork 血缘（`parentSession`）。"两条互不相干的对话算同一棵树"和"这条支线被
手动拆出去了"是**我们自己的概念**，任何日志里都没有，只能自己存：
`$DSH_HOME/plugins/dsh-tree/shape.json`。

```json
{ "version": 1, "groupOf": { "<会话>": "<树编号>" }, "detached": ["<会话>"] }
```

放 home 不放 localStorage：换浏览器、进手机都还在。跟 `/outlines` 一起下发，少一个往返，
也避免"大纲到了形状没到"那一帧的错分组。

**分组必须是主动登记的。** 在空节点上按 ＋ 开出来的新对话，才登记进当前这棵树
（`api.fresh` 拿到新 id 后立刻 `reshape`）。没登记过的对话各自成树。

> ⚠️ 试过"整个 cwd 全算一棵"：16 条未归档对话把导轨撑到 326px，而且三条毫不相干的
> 对话挤在同一个空节点下面。别再回去。

**分离**：剪的是**图上的边**，不是会话边界。

> ⚠️ 别按"是不是 fork 出来的新会话"来判断。节点 1 后面跟着 2/3/4/5 时，2 是会话自己的
> 下一轮、3/4/5 是 fork，但它们在图上都只是 1 的一个孩子 —— 按会话判断会让唯独 2 没有
> 分离按钮（踩过，test-highlight 用例 10 钉着）。

| | |
|---|---|
| **能不能剪** | 一路往上只要有**任一祖先有多个孩子**就能。`node.canDetach = parent.children.length > 1 &#124;&#124; parent.canDetach`，父在前子在后遍历时 O(1) 顺带算出来，不用点击时回溯，也不会因为别处新增/分离而过期 |
| **剪在哪** | 不是你点的那个节点。从它一路往上，父亲只有这一个孩子就继续上移，直到撞见有多个孩子的父亲 —— 剪点是它底下的那个节点（`cutPointOf`） |
| **剪完之后** | 新树 = 根到剪点父亲那段**路径（前缀，照抄）** + 剪点的整棵子树；旧树 = 原树扣掉它 |
| **站在哪棵** | 当前会话最深的那个节点所属的那棵；这条会话一轮都还没有就待在 root 那棵 |

> ⚠️ 剪在**被点的那个节点**上是错的。`1-2-{3-5, 4-6}` 里对 6 点分离，剪在 6 上会让 4
> 留在旧树，两棵树看着"没同步"（踩过，用例 11 钉着）。剪点该是 4：新树 `1-2-4-6`、
> 旧树 `1-2-3-5`，只共享照抄的前缀 `1-2`。
>
> `cutPointOf` 有值 ⟺ `canDetach` 为真，这条也钉着 —— 否则会出现"按钮在但点了没反应"。

`detached` 记的是**节点 key**（`<会话>:<轮次>`）。早先记的是纯会话 id，`cutSet()` 会把
老记录翻成它第一个自有轮次的节点，免得之前拆过的悄悄失效。

> ⚠️ 剪边必须排在**算列之前**，否则剪掉的子树还占着列宽，导轨白白变宽。

### 合并：两棵树并成一棵

分离的逆向操作有两个，走的是**两套存储**，别搞混。

| | 存哪 | 按钮 | 怎么确定结果 |
|---|---|---|---|
| **接回去** | `detached` | 剪缝节点上的 `⇤` | 分离时照抄了根到剪点父亲的前缀，**剪缝看得见**，接回哪儿一目了然 |
| **合并** | `groupOf` | 树根空节点上的 `⊕` | 只需要知道是**哪两棵树** |

合并**不需要指定"接到哪个节点"**。两棵树的节点互不相同（同一个问题重问一遍，答案也不会
一样），合完就是两条链并排挂在同一个空根下 —— 给定两棵树，结果唯一确定。所以挑单子里
**列的是树，不是节点**：按会话列的话，分支多的对话会刷屏，而且点哪一行结果都一样。

> ⚠️ **`groupOf` 必须在写入时压平。** `treeOf` 只查**一跳**（`groupOf[root] || root`），
> 不跟着链走。「A 合进 B，再把 B 合进 C」如果只写 `groupOf[B]=C`，就会变成
> `A→A`、`B→B+C` —— **先合进来的 A 无声无息掉出去**（实测过）。
> 所以 `reshape` 写 `groupOf[X]=T` 时要 ① 把 T 解析到终点 ② 把原本指着 X 的人一起改指过去。
> 存盘里的值恒为"终点"（不再是别人的 key），于是拆回去就是 O(1) 地删一条。
> 解析带 `seen` guard：手改坏的 shape.json 不许把写入挂死。

> ⚠️ **拖宿主的会话行是做不到的。** 那些行既没有 `data-session-*`，也没有留给单行的
> slot（只有 `sidebar.brand` / `footer` / `settings` / `workspaces` 那几个）。
> 不过本 cwd 的全部对话本来就在 `/outlines` 的答复里，**单子我们自己渲染**就行。

合并进来的对话在单子里变成 `⊖`，点一下拆回独立的一棵。分组仍然是**主动登记**的：
合多了照样会把导轨撑宽（早期无脑按 cwd 分组，16 条对话撑到 326px）。

> ⚠️ **有一头还在跑就不让合并**，被拦下的那条**留在单子里**灰掉、右边写原因
> （「这条对话还在运行，跑完再合」）。直接不显示的话，用户只会觉得"我那条对话怎么不见了"。
>
> 说清楚：合并本身**不危险**，它只写 `shape.json`，不碰对话。拦它是因为正在跑的那棵树
> **形状算不准** —— 它的撤回记录这会儿读不了（见上面那节），刚合进来就画错更难解释。
>
> 判据是三态的 `statusProbe`，**别压成布尔**：读旁车时 `unknown` 当成在跑（不读，赌错了
> 打死整轮对话），拦合并时 `unknown` 当成空闲（放行，赌错了只是多合了一条）。
>
> 一棵树只要**任何一条分支**在跑就拦。整棵树是一起并过来的，只看树根的话，
> 跑着的那条分支照样会被顺手带进来。

**对话内容的合并（git merge 那种）不做。** 对话是一条因果链，分支 B 的模型从来没看见过
分支 A 的那几轮，把两边的文字拼起来得到的是一段从未发生过的对话；走 dsh-claude 时更糟，
日志里 assistant 正文是空的，"合并"等于凭空造一个新的 Claude 会话再把正文灌进去。
真要用到另一条分支的结论，正经做法是在这条里**当成新的一轮说给模型听**。

### hover intent：让鼠标够得到自己那个 ＋

详情卡贴在导轨**左**外侧（右边是聊天区边缘，没地方放），＋ 在卡片右缘。于是鼠标从
节点走到 ＋ 必须**横穿左边每一列**。而列间距只有 17px、命中区却宽 22px ——

> ⚠️ 往左挪 5px 就进了左邻居的命中区，悬停被抢、卡片换人。结果就是
> **只要一个节点左边还有节点，它的 ＋ 就永远按不到**，按下去加的是左邻居的子树。

做法是业界标准的 hover intent（jQuery hoverIntent，2007）：**整条导轨只有一个
`mousemove`**，节点上不挂 `onMouseEnter`。

| 状态 | 行为 |
|---|---|
| 还没开卡片 | 碰到点立刻开（要跟手） |
| 卡片开着，压着同一个点 | 不动 |
| 卡片开着，压着别的点 | 起 `restMs`(140ms) 计时器；**任何一次移动都把它清掉** |

于是"手还在动"= 你在赶路，谁都抢不走卡片；"停下来"= 你真想选这个点，换过去。
横穿多少列都无所谓，这是几何上绕不开的问题，只能靠时间维度区分意图。

决策抽成纯函数 `hoverNext(hover, at)`，返回 `keep` / `now` / `rest`。

> ⚠️ 卡片开着时换目标**必须**返回 `rest`。改成 `now` 就等于退回挂 `onMouseEnter`
> 的老做法，＋ 立刻又够不着了（test-highlight 用例 6 钉着，改回去炸 9 条）。

**推翻过的方案**（别再试）：透明走廊 + 梯形 `clip-path` + "掉头就让位" + 超时拆除。
四套启发式互相兜底，实测仍够不着；而且 `handOver()` 让位时若算出"压着的还是当前这个
点"，`hover` 不变 → 依赖 `hover.node.key` 的 effect 不重跑 → `bridge` 永久停在
`false`，走廊再也不出现。鼠标右移抖 2px 就能触发。

### 没有鼠标的机器：iPad / iPhone / 触摸屏本

上面那一整套 hover intent 建立在一个前提上：**存在"滑过"这个状态**。
Windows + Chrome、macOS + 触控板都满足（触控板照样发 `mousemove`），但手指不满足。

> ⚠️ 改之前，iPad 上这个插件是这样的：点一下节点＝直接跳走，卡片**永远开不出来** ——
> 分支、收藏、改名、合并、分离这五件事一件也够不着，一个交互式的树退化成一张能点的图。
> 本机上永远测不出来，因为那些代码路径一条都不会走到。

判据不是嗅 UA（iPad 的 UA 里写着 Macintosh，根本分不清），而是问浏览器
**这块屏能不能悬停**：`matchMedia('(hover: hover)')`。查不出来一律当作"能" ——
判错成"不能"会让每台桌面机都要点两下，那是所有人都撞得上的退化；判错成"能"只是
触摸设备退回原状。Surface 这类两种指针都有的机器靠 `change` 事件实时切换。

能悬停的机器上，**一个字节的行为都没变**：下面这些分支全部不进。

| 鼠标上靠什么 | 手指上换成什么 |
|---|---|
| 滑到点上出卡片 | **点一下开卡片，再点同一个点才跳**（`tapNext`，和 iOS 自己对 `:hover` 菜单的规矩一致） |
| 鼠标移开 + 280ms 计时器关卡片 | 戳导轨以外的任何地方关（捕获阶段的 `pointerdown`） |
| `title` 说明灰掉的按钮为什么按不动 | 再挂一条 `onClick`，把同一句话摊在卡片里 |

> ⚠️ 触摸模式下那个容器 `mousemove` 必须**整个歇着**。iOS 为了兼容老页面，会在每次点击
> **之前合成一个 `mousemove`**；它会当场把卡片开在被戳的那个点上，紧接着的 `click`
> 一看"卡片已经停在我身上"就直接跳走了 —— 两下点的规矩当场作废，等于什么都没改。

> ⚠️ 同理 `release()`（那个 280ms 关卡片的计时器）在触摸模式下不许起来。iOS 在别处一戳
> 会补发一串合成鼠标事件（含 `mouseleave`），计时器一跑，卡片会在手指还没够到 ＋ 的时候
> 自己关掉。

#### WebKit 上不写就出毛病的几条（`pointer.js`）

`NO_ZOOM` 和 `TAPPABLE` 两张表，分工是硬性的：

- `touchAction: manipulation` —— 重点**不是**去掉双击缩放，而是 Safari 为了等
  "你是不是还要点第二下"会把 `click` 压后约 300ms，每个按钮手感发黏。
  顺带它让卡片的"双击展开"真的能用：否则那两下被当成缩放手势吃掉，`dblclick` 压根不发。
- `WebkitUserSelect` —— **React 的内联样式不补 vendor 前缀**，而 Safari 16.4 之前
  只认带前缀的那个。不补的话手指在导轨上一划就选中一片字，还弹出两个选择手柄。
  test-pointer 用例 5 扫全部 `src/client/*.js`，谁自己手写一份不带前缀的就炸。
- `WebkitTouchCallout` —— 长按不再弹"拷贝 / 共享"那张系统菜单。
- **`TAPPABLE` 绝不能盖到含 `<input>` 的容器上** —— iOS 上祖先的 `user-select: none`
  会连输入框里的字一起选不中，改名框放不了光标。卡片外壳只用 `NO_ZOOM`，这就是把
  这两张表拆开的全部理由。

#### 输入框字号必须 >= 16px

iOS Safari 的死规矩：聚焦一个字号小于 16px 的输入框，它把**整个页面放大过去，
而且不会自己缩回来**。导轨是 `position: fixed` 的，放大之后直接跑到屏幕外 ——
表现就是"改了个名字，树没了"。宿主的 viewport meta 不归我们管（改不了
`user-scalable`），所以只能从字号这一头解，且只在不能悬停时顶上去，
免得桌面上那个框忽然比周围大一圈。

#### 视觉视口（`watchViewport`）

Safari 的地址栏随滚动收起/展开、双指缩放，这两下都只动 **visual viewport**：
`window` 的 `resize` 一声不吭，`innerWidth` 纹丝不动。而导轨的坐标是
`getBoundingClientRect()` + `innerWidth` 算出来的 `position: fixed`，于是它悬在原地，
要等 800ms / 400ms 那个轮询兜底才跟上 —— 表现是"手一松，树晚半拍才挪过去"。
订上 `visualViewport` 的 `resize` + `scroll` 就跟手了。桌面浏览器上这两个事件基本不发，
**对 Windows / macOS 没有任何影响**。

#### host 半不用改

`src/host/*` 全走 `node:path` / `os.homedir()`，没有盘符、反斜杠、大小写假设；
`cwd` 的比较两头都来自 dsh 自己，格式天然一致。Mac / Linux 上开箱即用。

### 连线：两端都要在节点边缘停住

> ⚠️ 别把线画到**圆心**。节点填充是半透明的（路径上垫一层淡色、压缩节点垫橙色），
> 画到圆心的话线会从圆环 / 菱形正中间透出来 —— 一个橙色菱形中间插一条蓝线。

`reachFor(kind, active, dotSize, theme)` 算每一端让开多少：圆形让半径，**菱形要让 0.71×直径**
（正方形转 45° 后半高是半边长的 √2 倍），树根那个空节点画得大一圈也要多让。
折角处不让 —— 那里没有节点。

几何抽在纯函数 `segments()` 里，渲染只负责把它返回的矩形摆上去（test-highlight 用例 13
钉着：两端都不许压到节点圆心，折角必须在父节点那一行）。

> ⚠️ 线是 1px 宽的方块，`left` / `top` 要各减**半个线宽**。不减的话线占 `[x, x+1)`，
> 中心落在 `x+0.5`，而点的中心在 `x` —— 整条线整体偏右半像素，线性的树看着就是
> "一段线一个点"左右不对称（John 报的）。横段还要往折角那头多伸半个线宽，
> 否则拐角上缺个 0.5px 的口。

### 全树唯一的上色规则

John 问到了根子上：「我们说一个 node 的颜色是 xxx，到底是指这个 node 的哪里？
是边框？还是填充？」——**以前这个问题没有答案**，因为同一个色号会被五套不同的
规则画出五种样子：

| 画的地方 | 描边 | 填充 |
|---|---|---|
| 普通节点 | 主色 | **页面底色**（等于不填） |
| 当前路径 | 主色 **@0.9** | 主色 @0.18 |
| 压缩节点 | 主色 | 主色 @0.3 |
| 空节点 | 主色 | 主色 @0.15 |
| 收藏 | **另算的一个压深色** | 主色实心 |
| 设置里的选择器 | 主色 | 主色 @0.3 |

现在只有一条，写在 `paint()` 里：

```
普通态：描边 = 这个色        填充 = 这个色 @ FILL_ALPHA（0.18）
实心态：描边 = 这个色        填充 = 这个色（不透明）
```

**描边和填充永远是同一个颜色，差别只有一个透明度。** 于是"我设的那个颜色"
指的就是它，不存在第二个答案。这是 Material / Primer / Ant Design 这些成熟设计
系统的通行写法：一个 accent 令牌，填充是它的低透明度版，选中时填实。

- **实心态只有一个含义：正看着这一轮。** 别再往里塞第二个含义 —— 收藏、压缩、
  空节点靠颜色和形状表达自己，不靠填不填实。
- **实心底上的内容要换 on-color**（`onAccent`）。自定义文字那个框填实之后，
  字必须换成和底色对比最大的那个，否则**恰好是最该看清的那一轮最糊**。
- **外发光也是同一个色**。以前它另取"这个 kind 在当前路径上的颜色"，于是一个
  配成绿色的普通节点，滚到它那一轮时会发蓝光 —— 又一个对不上的颜色。
- **`ROLES` 里不许再出现透明度数字**。深浅归 `paint`，那儿只有一个 `FILL_ALPHA`。
- **颜色原样用，画的时候不许动它。** 明暗自适应属于"默认值"那一层（`PALETTE` 分两版），
  不属于"画"这一层 —— 否则用户在设置里挑的色号和他在树上量到的对不上。
- **选择器画的就是节点平时的样子**（同一个 `paint`，非实心那一档），所以在设置里
  挑到的和树上画出来的必然一致。

用例 18 逐条钉着上面每一句，其中最关键的一条是：**同一个色号，普通节点和收藏节点
必须长得一模一样**（形状除外）。那条直接对应 John 报的"色差"。

### 颜色与形状可配

四个角色各一组颜色 + 形状：**普通节点 / 当前路径 / 压缩节点 / 空节点**，存在 host 的
settings 里。

> ⚠️ 压缩和空节点**自带颜色**，不跟着"在不在当前路径上"变。它们靠颜色表明自己**是什么**，
> 不是表明自己**在哪**；跟着 `active` 切的话，那两个设置就只在某些时候生效，等于半个死设置。

> ⚠️ 空节点的**虚线边不跟着配置走** —— 那是"还没说话"的记号。方框类靠
> `borderStyle: dashed`，多边形只能自己描 `strokeDasharray`（用例 15 钉着）。

`THEME` 里每加一个角色，`FIELDS` 和 `ROWS` 都得跟着加 —— 用例 15 有一条结构不变式守着，
光改 `THEME` 不加设置项会直接炸。

| | |
|---|---|
| 形状 | 见下面那张表，或 `char:<字>` / `img:<哈希>` |
| 颜色 | `#rrggbb`。**派生色一律用 `fade()` 现算** —— 路径垫色、外发光、连线都从主色派生 |

#### 12 个预设形状

| 画法 | 形状 |
|---|---|
| `border-radius` | `circle` / `rounded` / `square` |
| `border-radius` + `rotate(45deg)` | `diamond` |
| `<polygon>` | `triangle` / `triangle-down` / `chevron` / `pentagon` / `hexagon` / `cross` / `sparkle` / `bowtie` |

挑形状的判据是**11px 上还分得出来**（默认节点就这么大）。所以六边形取**平顶**那一版
（尖顶六边形在这个尺寸上和圆几乎一样，那条横边是它唯一的辨识点），四角星而不是五角星
（五角星是收藏的专属记号，两件事不能撞脸），正八边形干脆没加（就是个圆）。

顶点**一律算出来**：`regularPoly(n, turn)` 出正 n 边形，`crossPoly(t)` 出十字，
`starPoly(n, inner)` 出星。`grow` 也交给 `growOf()` 按面积配齐，不再手抄两位小数。

> ⚠️ 以前 `grow` 是注释里算一遍、代码里抄一个两位小数（三角 1.34、星星 1.67）。
> 加一个新形状就得再手算一次面积比，而算错的那个"看着小一圈"没有任何断言会响。

> ⚠️ `polyArea` 是鞋带公式，**只对简单多边形有效**。沙漏第一版写成了
> "左上→右下→左下→右上"那个自交四边形 —— 画出来是对的，但两个三角朝向相反、
> 面积正好抵成 `1.4e-17`（**不是 0**，所以 `area <= 0` 那道闸拦不住），
> `growOf` 除一下得到 **2.4 亿倍**，那个点会占满整块屏。
> 现在沙漏带个腰画成简单多边形，`growOf` 的判据也改成 `<= 1e-6`。
> 用例 24 有两条专门盯着这个：每个形状面积都 > 0.05、每个 `grow` 都 < 3。

> ⚠️ 派生色不许再硬编码。用户把主色改成绿的之后，还留着 `rgba(88,166,255,…)`
> 就会绿一半蓝一半（用例 12 钉着，硬编码回蓝会炸 3 条）。

`shapeSpec(want)` 是唯一的解析口：预设查 `SHAPES`，`char:` 开头的取后面那最多
`GLYPH_MAX`（5）个**码点**当字形，其余一律退回圆形。（收藏那条路走 `favShape()`，退回的是星不是圆 —— 见上面那节。）host 的 schema 只声明成 `Schema.string()`，合法值由浏览器半的
`FIELDS.accept` 把关：存进来一个认不得的值应该退回默认，而不是把树搞崩。

卡片上**一个角色一行，左边颜色右边形状**（`ROWS` 表）。形状选择器里的每个按钮直接用
`preview()` 画出那个形状本身 —— 和树上的点共用 `shapeSpec`，所以选择器上看到的就是
节点将来的样子，按钮上不写"圆形""菱形"这种字。预设后面跟两格自定义：传图片，或者
填一个字符。

**写设置要先本地回显。** `store.set` 得绕 host 转一圈，拖色板时那一圈跟不上手，读数和
预览会一直停在旧值上 —— 看着就是"调完下面没跟着变"（John 报的）。卡片自己存一份
`draft`，点下去立刻画上，等设置真的回到这个值再撤掉；写失败也撤掉，别让界面撒谎。

### 自定义节点图片

用户挑一个文件 → **浏览器半 `shrink()` 过一遍 canvas** → 96×96 的 PNG → POST 给 host 半
存进 `$DSH_HOME/plugins/dsh-tree/icons/<内容哈希>.png` → 设置里存 `img:<哈希>`。

> ⚠️ **绝不能直接落用户原文件。** SVG 里可以写 `<script>`，原样挂到同源地址上再当图片
> 引就是个后门。过一遍 canvas 就只剩像素了；host 半再验一次 PNG 魔数，防着有人绕开
> 前端直接 POST。

> ⚠️ 图片 id 要直接拼成文件名和 URL，校验必须是**白名单**（`/^[0-9a-f]{32}$/`）。
> 别改成"过滤掉 `..` 就行"那种黑名单写法 —— 路径穿越就是这么来的（test-icon 用例 2 钉着）。

| | |
|---|---|
| 尺寸 | **不限制，自动换算**。等比缩进方框（contain），空出来的地方透明补齐，竖图横图都不变形 |
| 为什么是 96 | 点最大 `Z.dot(11) × 缩放 250%` = 27.5px，悬停再 ×1.4 ≈ 38.5px，二倍屏 77 个物理像素 |
| 为什么用内容哈希 | 同一张图传两次是同一个文件，来回换不会堆出好几份 |
| 清理 | 只留最近 32 张。**每次被取走都会刷新 mtime**，所以"正在用的"每次开页面都会浮到最前面 |

### 连线：同一行上一像素只画一次

**症状**：一个父节点带好几个孩子时，横线一会儿粗一会儿细，还上下起伏（John 报的）。

**为什么**：走法是先横后竖，于是那个父节点的**每个孩子各画一条横段** —— 都贴在
父节点那一行、右端都停在父节点边上，只是往左伸的长度不同。也就是一组**同心嵌套**的
线段，靠父节点那一截被画了 N 遍：

- 每条 `<span>` 带着自己的 `opacity`（鱼眼淡出用的），叠 N 层就比别处黑一截 → 忽粗忽细
- `top` 是 `yFrom - 0.5`，通常是小数，浏览器把这 1px 抹到**两行**物理像素上；
  叠几层之后那两行都被填实 → 上下起伏

**怎么修**：`trimRuns` —— 不靠"谁后画谁赢"，直接让这些横段**互不重叠**。
按优先级挨个来，每条只画还没被占掉的那截，画完把自己占的区间记下。
嵌套的那几条里第一条占完，剩下的整条都被盖住，一条都不画。

优先级：**蓝的（当前路径）先占，同色里长的先占**。
蓝的先占 = 靠父节点那一截归蓝线，和原来"蓝线压在最上面"看到的是同一个结果；
区别是现在每个像素只被画一次，所以既不依赖 DOM 顺序，也不会叠出更黑的一段。

- ⚠️ 记下的是**原来那整条**，不是画出来的那几截。被当成残渣（< `MIN_RUN`）丢掉的
  那点宽度也得算占过，否则后面每一条都会在同一个位置再补一根发丝线。
- ⚠️ 按 `top` 分组，不是按父节点分组 —— 同一行上不同父节点的横段照样会撞。
- 竖段各在各的列上，本来就不会互相盖，所以不进这一套。
- 去重**不许把线弄短、也不许在中间裂缝**：test-layout 用例 5 拿并集和逐点扫描钉着。

### 连线：先横后竖

跨列那条折线**必须先横向挪到自己那一列、再往下走**。

> ⚠️ 反过来（先竖后横）时，从节点 2 岔到节点 4 的竖线会在第 0 列从 2 一路压到节点 3
> 头上再拐弯，看着像"经过 3 之后转个弯到 4"。

### 和原生轮次导轨的关系

原生那条只管当前会话的轮次，我们这条是它的超集，所以把原生那条藏掉
（从它自己注入的 `<style data-plugin-css*="TurnNavigator.module.css">` 里正则出带哈希的类名前缀）。
取不到就不藏 —— fail-open，宁可两条并存也不要整个导轨崩掉。

---

## 6. 测试

十二个离线脚本，都不用开浏览器（`npm test` 会先构建再一起跑）。
**每个断言都验证过"能抓住对应的 bug"**（把修复退回去会当场炸）。

```bash
node test.mjs                 # 拿真实会话日志跑整条渲染管线，--print 打印 ASCII 树
node test-highlight.mjs       # 手捏的小树，钉死高亮的边界情形 + 样式 key 集合
node test-elide.mjs           # 省略的距离、行号压实、缩放档位
node test-icon.mjs            # 自定义节点图片：只收 PNG、内容哈希、清理不误伤
node test-rewind.mjs          # 撤回：done 的口径、区间比对、成图、旁车读写
node test-merge.mjs           # 合并 / 接回去：分组可传递、挑单子列树不列节点、在跑就拦
node test-shape.mjs           # 拿**真实会话**跑合并 / 分离：分离再接回逐节点复原
node test-http.mjs             # 路由外壳：方法分发、出错码、图片不许被 JSON 编码
node test-lifecycle.mjs       # 启用 / 停用 / 再启用：两半都不许留东西
node test-pointer.mjs         # 跨设备：悬停判据、手指语义、WebKit 必补的样式、卡片按住、「字」框的输入法规矩
node test-layout.mjs          # 版式：列距跟着最宽形状、空当里居中、横段去重、被别的插件盖住就收起
DSH_HOME_REAL='...' node test-branch.mjs   # 把真实分支倒带到"刚出生"那一刻，重放接管逻辑
```

全都从 `client.js` 的 `__pure` 出口或 `index.js` 的 `__test` 出口取函数 ——
**测的是真代码，不是复制品**。共用的断言/加载器在 `test-kit.mjs`。

浏览器控制台里 `__dshTree()` 可以把当前树的真实状态倒出来（每个节点的蓝/白、血缘、岔路点、归档集）。

---

## 7. 还没做

- 节点名字存在 localStorage，换浏览器就没了；应该挪到 host 的 `ctx.storage.domain`。
- 父会话正在跑时接不上上下文，只能排队等它空下来补（见上）——**分支本身照开**，
  只是那几秒里没有继承的记忆。真正的修法在上游：dsh-claude 的 `#writeNow`
  对 rename 没有任何 EPERM 重试，异常直接被当成"Claude Code 掉线"。
  Windows 上杀毒、Search 索引、网盘同步都可能踩到同一个坑，值得给它报个 issue。
- 左侧会话列表仍是平的（宿主 SPA，没有可用的 slot，也没有 `data-session-*` 钩子），
  一个对话有几条分支就占几行。
- 超过约 60 轮的对话在最小行高下会溢出。
- 新分支第一轮仍会带上宿主注入的 runtime-context 快照（优先级低）。
- fork 的两个缺陷本质上该在上游修：切点应落在 `turn/end`，而不是"下一轮 `turn/start` 之前"。
